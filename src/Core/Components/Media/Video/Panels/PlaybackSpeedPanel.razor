@namespace FluentUI.Blazor.Community.Components
@using System.Globalization

<FluentDialogBody>
    <FluentStack Orientation="Orientation.Vertical"
                 VerticalGap="0"
                 Style="width: 95%">
        <FluentSlider Min="0.25"
                      Max="2.0"
                      Step="0.05"
                      Label="@(string.Format(Labels.PlaybackSpeedLabelCustomFormat, (double)Content.Speed))"
                      AriaLabel="@(string.Format(Labels.PlaybackSpeedLabelCustomFormat, (double)Content.Speed))"
                      @bind-Value=Content.Speed
                      Style="margin-bottom: 20px" />

        <FluentRadioGroup @bind-Value=Content.Speed
                          Orientation="Orientation.Vertical"
                          Label="@Labels.PlaybackSpeedLabel"
                          AriaLabel="@Labels.PlaybackSpeedLabel"
                          Style="margin-bottom: 20px">
            <FluentRadio Value=0.25>
                @(0.25.ToString(Culture))x
            </FluentRadio>
            <FluentRadio Value=0.50>
                @(0.50.ToString(Culture))x
            </FluentRadio>
            <FluentRadio Value=0.75>
                @(0.75.ToString(Culture))x
            </FluentRadio>
            <FluentRadio Value=1>
                @Labels.NormalPlaybackSpeedLabel
            </FluentRadio>
            <FluentRadio Value=1.25>
                @(1.25.ToString(Culture))x
            </FluentRadio>
            <FluentRadio Value=1.50>
                @(1.50.ToString(Culture))x
            </FluentRadio>
            <FluentRadio Value=1.75>
                @(1.75.ToString(Culture))x
            </FluentRadio>
            <FluentRadio Value=2>
                @(2.ToString(Culture))x
            </FluentRadio>
        </FluentRadioGroup>
    </FluentStack>
</FluentDialogBody>
