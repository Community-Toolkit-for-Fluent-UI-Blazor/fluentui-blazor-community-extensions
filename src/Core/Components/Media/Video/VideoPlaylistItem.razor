@namespace FluentUI.Blazor.Community.Components
@inherits FluentComponentBase

<div class="audio-playlist-item"
     style="@Style"
     id="@Id"
     @onclick="OnHandleClickAsync">
    <FluentStack Style="height: 100%"
                 VerticalAlignment="VerticalAlignment.Center">
        @if (Track is not null)
        {
            @if (!string.IsNullOrEmpty(Track?.Metadata?.Visual?.ThumbnailUrl))
            {
                <div style="width: 48px; height: 48px; border-radius: 8px; background-color: white">
                    <img width="48"
                         height="48"
                         src="@Track?.Metadata?.Visual?.ThumbnailUrl"
                         title="@Track?.Metadata?.Descriptive?.Title"
                         aria-label="Track?.Metadata?.Descriptive?.Title" />
                </div>
            }
            else
            {
                <div style="width: 48px; height: 48px; border-radius: 8px; background-color: var(--accent-fill-rest)" />
            }

            <FluentText Color=Color.Primary
                         Style="font-size: 18px"
                         Weight="TextWeight.Bold">@(Track?.Metadata?.Descriptive?.Title ?? UnknownVideoTitle) </FluentText>

            <FluentSpacer />

            <FluentIcon Value="@TrackIcon" />
        }
    </FluentStack>

    <FluentDivider />
</div>
