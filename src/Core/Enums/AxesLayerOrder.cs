namespace FluentUI.Blazor.Community.Components.Enums;

/// <summary>
/// Specifies the visual layer of a axes component, such as background or foreground, used to control rendering order
/// and appearance.
/// </summary>
public enum AxesLayerOrder
{
    /// <summary>
    /// The axes are rendered in the background layer, behind other content.
    /// </summary>
    Background,

    /// <summary>
    /// The axes are rendered in the foreground layer, in front of other content.
    /// </summary>
    Foreground
}

