@namespace FluentUI.Blazor.Community.Components
@inherits FluentComponentBase

<FluentStack Id="@Id"
             Class="@Class"
             Style="@Style"
             VerticalAlignment="VerticalAlignment.Center"
             @onclick="OnHandleClickAsync">
    @if (Track is not null)
    {
        @if (!string.IsNullOrEmpty(Track.Cover))
        {
            <img width="48"
                 height="48"
                 src="@Track.Cover"
                 title="@Track.Title"
                 aria-label="@Track.Title" />
        }

        <FluentStack VerticalGap=1 Orientation="Orientation.Vertical">
            <FluentLabel Color=Color.Accent Style="font-size: 18px" Weight="FontWeight.Bold">@Track.Title</FluentLabel>
            <FluentLabel Color="Color.FillInverse">@Track.Artist</FluentLabel>
        </FluentStack>
    }
</FluentStack>
