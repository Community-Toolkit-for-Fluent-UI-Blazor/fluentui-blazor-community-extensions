@using FluentUI.Blazor.Community.Components.Charts.Series
@using FluentUI.Blazor.Community.Components.Charts.Themes
@using FluentUI.Blazor.Community.Components.Enums
<p>
    This example demonstrates how to use some category charts with sample data.
    You can select the chart palette style and the chart category to see how the chart changes.
</p>

<div style="width: 100%; height: 500px">
    <FluentStack Height="100%" Orientation="Orientation.Vertical">
        <FluentStack HorizontalGap="4px">
            <FluentSelect Items="@s_chartPaletteStyles"
                          @bind-Value="_selectedChartPaletteStyle" />

            <FluentSelect Items="@s_chartCategory"
                          @bind-Value="_selectedChartCategory"
                          @bind-Value:after="OnCategoryChanged"/>
        </FluentStack>

        <FluentCxChart @ref="_ref" PaletteStyle="@_selectedChartPaletteStyle">
            @if (_selectedChartCategory == ChartType.CategoryArea)
            {
                <AreaSerie Name="Area" Items="@(_items)" />
                <AreaSerie Name="Area 2" Items="@(_items2)" />
                <AreaSerie Name="Area 2" Items="@(_items3)" />
            }
            else if (_selectedChartCategory == ChartType.CategoryLine)
            {
                <LineSerie Name="Line" Items="@(_items)" />
                <LineSerie Name="Line 2" Items="@(_items2)" />
                <LineSerie Name="Line 2" Items="@(_items3)" />
            }
            else if (_selectedChartCategory == ChartType.Column)
            {
                <ColumnSerie Name="Columns" Items="@(_items)" />
                <ColumnSerie Name="Columns 2" Items="@(_items2)" />
                <ColumnSerie Name="Columns 2" Items="@(_items3)" />
            }
            else if (_selectedChartCategory == ChartType.Bar)
            {
                <BarSerie Name="Bars" Items="@(_items)" />
                <BarSerie Name="Bars 2" Items="@(_items2)" />
                <BarSerie Name="Bars 2" Items="@(_items3)" />
            }
            else if (_selectedChartCategory == ChartType.StackedBar)
            {
                <StackedBarSerie Name="Bars" Items="@(_items)" />
                <StackedBarSerie Name="Bars 2" Items="@(_items2)" />
                <StackedBarSerie Name="Bars 2" Items="@(_items3)" />
            }
            else if (_selectedChartCategory == ChartType.StackedColumn)
            {
                <StackedColumnSerie Name="Columns" Items="@(_items)" />
                <StackedColumnSerie Name="Columns 2" Items="@(_items2)" />
                <StackedColumnSerie Name="Columns 2" Items="@(_items3)" />
            }
            else if (_selectedChartCategory == ChartType.StackedArea)
            {
                <StackedAreaSerie Name="Area" Items="@(_items)" />
                <StackedAreaSerie Name="Area 2" Items="@(_items2)" />
                <StackedAreaSerie Name="Area 3" Items="@(_items3)" />
            }
            else if (_selectedChartCategory == ChartType.Stacked100Bar)
            {
                <Stacked100BarSerie Name="Bars" Items="@(_items)" />
                <Stacked100BarSerie Name="Bars 2" Items="@(_items2)" />
                <Stacked100BarSerie Name="Bars 2" Items="@(_items3)" />
            }
            else if (_selectedChartCategory == ChartType.Stacked100Column)
            {
                <Stacked100ColumnSerie Name="Columns" Items="@(_items)" />
                <Stacked100ColumnSerie Name="Columns 2" Items="@(_items2)" />
                <Stacked100ColumnSerie Name="Columns 2" Items="@(_items3)" />
            }
            else if (_selectedChartCategory == ChartType.Stacked100Area)
            {
                <Stacked100AreaSerie Name="Area" Items="@(_items)" />
                <Stacked100AreaSerie Name="Area 2" Items="@(_items2)" />
                <Stacked100AreaSerie Name="Area 3" Items="@(_items3)" />
            }
        </FluentCxChart>
    </FluentStack>
</div>


@code {
    private FluentCxChart? _ref;
    private static ChartPaletteStyle[] s_chartPaletteStyles = Enum.GetValues<ChartPaletteStyle>().ToArray();
    private ChartPaletteStyle _selectedChartPaletteStyle = ChartPaletteStyle.Autumn;
    private ChartType _selectedChartCategory = ChartType.Bar;

    private static ChartType[] s_chartCategory = [
        ChartType.CategoryArea,
        ChartType.CategoryLine,
   //     ChartType.CategoryStep,
        ChartType.Column,
        ChartType.Bar,
        ChartType.StackedBar,
        ChartType.StackedColumn,
        ChartType.StackedArea,
        ChartType.Stacked100Bar,
        ChartType.Stacked100Column,
        ChartType.Stacked100Area
    ];

    private static CategoryItem[] _items = new[]
    {
        new CategoryItem { Category = "Category 1", Value = 15 },
        new CategoryItem { Category = "Category 2", Value = 25 },
        new CategoryItem { Category = "Category 3", Value = 20 },
        new CategoryItem { Category = "Category 4", Value = 30 }
    };

    private static CategoryItem[] _items2 = new[]
    {
        new CategoryItem { Category = "Category 1", Value = 35 },
        new CategoryItem { Category = "Category 2", Value = 20 },
        new CategoryItem { Category = "Category 3", Value = 45 },
        new CategoryItem { Category = "Category 4", Value = 25 }
    };


    private static CategoryItem[] _items3 = new[]
    {
        new CategoryItem { Category = "Category 1", Value = 25 },
        new CategoryItem { Category = "Category 2", Value = 30 },
        new CategoryItem { Category = "Category 3", Value = 35 },
        new CategoryItem { Category = "Category 4", Value = 40 }
    };


    private void OnCategoryChanged()
    {
        _ref?.Reset();
    }
}
